import { DynamicResponsiveTableDirective } from './dynamic-responsive-table.directive';

describe('DynamicResponsiveTableDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicResponsiveTableDirective();
    expect(directive).toBeTruthy();
  });
});
